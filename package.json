{
  "name": "chirp",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit -p packages/core/tsconfig.test.json && tsc --noEmit -p apps/api/tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "typescript": "^5"
  }
}
